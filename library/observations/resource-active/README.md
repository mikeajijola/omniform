# Resource active assertion

An experimental deterministic observation that is satisfied when deployment state reports a named resource as active. It produces an evidence reference such as `resource:support_agent`.
