# Capabilities and actors

`approve_plan` is a capability. A button, agent tool, API operation, or machine command is an interface. The founder, service, AI agent, or robot is an actor. The runtime implementation, authorization policy, resulting state, and audit evidence remain shared. This separation avoids duplicating domain truth for each interface.
