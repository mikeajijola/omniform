# Apply

Apply executes only authorized plan changes and records per-change results and evidence. Partial apply is valid: applied, pending, failed, and unresolved work remain explicit. An implementation MUST NOT imply that a partial result fully realizes the desired company.
