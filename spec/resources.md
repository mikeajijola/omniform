# Resources

A Resource is a proposed or known implementation that may realise capabilities. Initial categories are `agent`, `person`, `team`, `workflow`, `connector`, `system`, `partner`, and `entity`. A person and an agent may realise the same capability without changing that capability's meaning.
