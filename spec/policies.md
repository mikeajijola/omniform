# Policies

A Policy defines when an operation is allowed or required, including approval and authority constraints. Policy is separate from interface and implementation; every actor invoking the same capability is subject to the same policy semantics.
