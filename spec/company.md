# Company

A Company is desired organisational intent: a stable `id`, human-readable `name`, purpose, outcomes, capabilities, resources, policies, and observation definitions. It may describe a new organisation or re-found an existing one. It does not contain runtime status, provider identifiers, credentials, findings, or calculated capability state.
