# Company

A Company is desired organisational intent: a stable `id`, human-readable `name`, purpose, outcomes, capabilities, resources, policies, and monitor definitions. It may describe a new organisation or re-found an existing one. It does not contain runtime status, provider identifiers, credentials, or calculated capability state.
